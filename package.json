{
  "name": "dat-simple-server",
  "version": "2.0.0",
  "description": "",
  "main": "index.js",
  "bin": {
    "dat-simple-server": "bin/server",
    "dss": "bin/server"
  },
  "scripts": {
    "build": "tsc --project . && ef-tspm",
    "postversion": "cp -r ./{package.json,src,bin} lib/ && npm publish lib/ && rm -rf lib/ && git push --follow-tags"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "yargs": "^14.2.0"
  },
  "devDependencies": {
    "@ef-carbon/tspm": "^2.2.5",
    "@types/node": "^13.13.52",
    "typescript": "^3.7.5"
  }
}
