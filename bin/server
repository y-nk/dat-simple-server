#!/usr/bin/env node

const { default: server } = require("../server");

const argv = require("yargs").argv;

const root = argv.public ?? `${process.cwd()}/www`;
const port = argv.port ?? 8001;

server(root, port);
